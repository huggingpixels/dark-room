import { Routes } from '@angular/router';
import { CharacterBuildComponent } from './character-build/character-build';

export const routes: Routes = [
    { path: '', component: CharacterBuildComponent },
];
